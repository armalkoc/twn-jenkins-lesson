pipeline {
    agent any
    tools {
        Maven "maven-3.9"
    }

    parameters {
        string(name: 'App Version', defaultValue: '', description: 'Specify application version')
    }

    stages {
        stage("build") {
            steps {
                echo "Builidng Application ..."

            }
        }

        stage("Test") {
            steps {
                echo "Testing application ..."

            }

        }

        stage("deploy") {
            steps {
                echo "Deployinh Application ..."
            }
        }
    }
}