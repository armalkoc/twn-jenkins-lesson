pipeline {
    agent any
    tools {
        maven "maven-3.9"
    }

    parameters {
        string(name: 'AppVersion', defaultValue: '', description: 'Specify application version')
        choice(name: 'AppVersion', choices: ['1.1.0', '1.2.0', '1.3.0', '1.4.0', '1.5.0'], description: 'these are available versions')
    }

    stages {
        stage("build") {
            steps {
                echo "Builidng Application number ${params.AppVersion} ..."

            }
        }

        stage("Test") {
            steps {
                echo "Testing application ..."

            }

        }

        stage("deploy") {
            steps {
                echo "Deploying Application ..."
            }
        }
    }
}